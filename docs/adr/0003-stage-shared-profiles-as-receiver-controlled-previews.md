# Stage shared profiles as receiver-controlled previews

Opening a Share URL creates an editable Preview instead of replacing the local Profile; exporting and resharing use the current Preview, while replacement requires an explicit Import. Future Sight is excluded from the shared payload and remains the receiver's choice, including warnings and reveal behavior. This favors local-data and spoiler safety over one-click import convenience.
